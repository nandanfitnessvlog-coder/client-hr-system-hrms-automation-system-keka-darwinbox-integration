rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // ── Universal Access for Dev Sprint ─────────────────────────────
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
